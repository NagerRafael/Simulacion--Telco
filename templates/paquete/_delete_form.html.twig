<form method="post" action="{{ path('app_paquete_delete', {'idPaquete': paquete.idPaquete}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paquete.idPaquete) }}">
    <button class="btn">Delete</button>
</form>
